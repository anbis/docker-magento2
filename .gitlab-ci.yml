stages:
  - build

build-image:
  image: docker:latest
  stage: build
  only:
    - master
  script:
    - docker build -t $IMAGE $IMAGE_PATH
    - docker tag $IMAGEE $REGISTRY/$IMAGE:$CI_COMMIT_REF_NAME
    - docker push $REGISTRY/$IMAGE:$CI_COMMIT_REF_NAME